# Core Beliefs

- Deterministic ingestion remains the single source of truth for every downstream agent.
- Analyst independence matters more than model diversity branding; the architecture should scale by count and configuration.
- Intermediate transforms must be explicit and auditable, especially when they suppress or redact signal such as prior RUCAM scores.
- Optional agents should be enabled by configuration without changing the default workflow contract.
